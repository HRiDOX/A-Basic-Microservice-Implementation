apiVersion: v1
kind: Pod
metadata:
  name: posts
spec:
  containers:
    - name: posts
      image: hridoy/posts:0.0.1
      imagePullPolicy: Never
#this file is for creating single pod now we don't need this we can create deployment to create post pod automatically      